services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SITE_URL: http://localhost:8080
    ports:
      - "8080:3000"
    environment:
      APP_API_URL: ${APP_API_URL:-}
